<h1>Listing users</h1>

<table>
  <tr>
  	<th>ID</th>
  	<th>Email</th>
  	<th>Subdomain</th>
  	<th>Default Appointment Length</th>
  	<th>Booking Advance</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
  	<td><%= user.id %></td>
  	<td><%= user.email %></td>
  	<td><%= user.subdomain %></td>
  	<td><%= user.default_appointment_length %></td>
  	<td><%= user.advance_booking_days %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', '#' %></td>
    <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', '#' %>
